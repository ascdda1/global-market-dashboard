'use client';

import { useEffect, useRef, useState } from 'react';
import EconomicCalendar from './economic-calendar';
import MarketInformation from './market-information';
import { assetLabels, macroLabels, moduleLabels, sessionLabels, statusLabels, tableLabels, type BilingualLabel } from './bilingual-labels';
import { createStockItem, marketItems, type ApiQuote, type MacroQuote, type MarketApiResponse, type MarketDataStatus, type MarketItem } from './market-data';

const defaultWatchlist = ['NVDA', 'MU', 'TSLA', 'AAPL'];
const primaryEquitySymbols = ['QQQM', 'SPYM', 'DIA', 'SMH', 'VGT', 'AVGO', 'NVDA'];
const watchlistStorageKey = 'marketflow-watchlist';
const maxWatchlistSize = 30;
const symbolPattern = /^[A-Z][A-Z0-9.-]{0,9}$/;
const historyRanges = ['1D', '1W', '1M', 'YTD'];

const fallbackValues: Record<string, ApiQuote> = {
  '^DJI': { value: 38627.99, change: 184.12, percent: 0.48, source: 'Fallback / Mock', updatedAt: 0 },
  '^GSPC': { value: 5432.26, change: 32.41, percent: 0.6, source: 'Fallback / Mock', updatedAt: 0 },
  '^IXIC': { value: 17608.44, change: 141.85, percent: 0.81, source: 'Fallback / Mock', updatedAt: 0 },
  'GC=F': { value: 2342.8, change: -8.4, percent: -0.36, source: 'Fallback / Mock', updatedAt: 0 },
  'CL=F': { value: 78.65, change: 1.12, percent: 1.44, source: 'Fallback / Mock', updatedAt: 0 },
  QQQM: { value: 210.24, change: 1.31, percent: 0.63, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 208.93 },
  SPYM: { value: 67.42, change: 0.28, percent: 0.42, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 67.14 },
  DIA: { value: 427.16, change: 1.89, percent: 0.44, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 425.27 },
  SMH: { value: 248.77, change: 2.14, percent: 0.87, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 246.63 },
  VGT: { value: 682.15, change: 3.94, percent: 0.58, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 678.21 },
  AVGO: { value: 186.68, change: 2.52, percent: 1.37, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 184.16 },
  NVDA: { value: 131.88, change: 3.74, percent: 2.92, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback', session: 'closed', previousClose: 128.14 },
};

const treasuryDefinitions = [
  { symbol: 'US2Y', name: 'US 2Y', category: 'Treasury', spark: 'M2 28 C25 25 48 30 69 20 S100 16 120 12' },
  { symbol: 'US10Y', name: 'US 10Y', category: 'Treasury', spark: 'M2 18 C22 25 44 22 64 31 S94 25 120 36' },
  { symbol: 'US30Y', name: 'US 30Y', category: 'Treasury', spark: 'M2 33 C24 29 42 34 63 25 S95 18 120 23' },
];
const cryptoDefinitions = [
  { symbol: 'bitcoin', name: 'Bitcoin', category: 'Crypto', spark: 'M2 34 C22 27 43 32 63 17 S92 26 120 6' },
  { symbol: 'ethereum', name: 'Ethereum', category: 'Crypto', spark: 'M2 21 C25 30 46 19 63 29 S95 12 120 19' },
];
const macroDefinitions = [
  { key: 'cpi', name: 'CPI', subtitle: 'Consumer prices' }, { key: 'coreCpi', name: 'Core CPI', subtitle: 'Ex food and energy' }, { key: 'pce', name: 'PCE', subtitle: 'Personal consumption' }, { key: 'corePce', name: 'Core PCE', subtitle: 'Ex food and energy' }, { key: 'nonfarmPayrolls', name: 'Nonfarm Payrolls', subtitle: 'US employment' }, { key: 'unemploymentRate', name: 'Unemployment Rate', subtitle: 'US labor market' }, { key: 'gdp', name: 'GDP', subtitle: 'US output' }, { key: 'effectiveFedFundsRate', name: 'Fed Funds Rate', subtitle: 'Effective rate' },
];
const summaryMacroKeys = ['cpi', 'coreCpi', 'unemploymentRate', 'gdp', 'effectiveFedFundsRate'];
const fallbackMacroValues: Record<string, MacroQuote> = {
  cpi: { value: 320.25, change: 0.3, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'index' },
  coreCpi: { value: 330.1, change: 0.3, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'index' },
  pce: { value: 126.8, change: 0.2, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'index' },
  corePce: { value: 124.9, change: 0.2, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'index' },
  nonfarmPayrolls: { value: 159_000, change: 118, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'thousands' },
  unemploymentRate: { value: 4.1, change: 0, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'percent' },
  gdp: { value: 30_100, change: 140, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'billions' },
  effectiveFedFundsRate: { value: 4.33, change: 0.01, referencePeriod: 'Fallback', source: 'Fallback / Mock', updatedAt: 0, unit: 'percent' },
};

function formatNumber(value: number) { return value.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
function formatSigned(value: number) { return `${value >= 0 ? '+' : ''}${value.toFixed(2)}`; }
function formatPercent(value: number) { return `${value >= 0 ? '+' : ''}${value.toFixed(2)}%`; }
function formatTime(timestamp: number) { return timestamp ? new Intl.DateTimeFormat('en-GB', { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false }).format(timestamp) : 'Awaiting live data'; }
function getFallbackQuote(symbol: string): ApiQuote {
  const knownQuote = fallbackValues[symbol];
  if (knownQuote) return knownQuote;
  const seed = [...symbol].reduce((total, character) => total + character.charCodeAt(0), 0);
  const value = 50 + (seed % 950); const change = ((seed % 401) - 200) / 100;
  return { value, change, percent: (change / value) * 100, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback' };
}

function buildItems(quotes: Record<string, ApiQuote>, stockSymbols: string[], history: Record<string, number[]> = {}): MarketItem[] {
  return [...marketItems, ...stockSymbols.map(createStockItem)].map((item) => {
    const quote = quotes[item.symbol] ?? getFallbackQuote(item.symbol);
    return { ...item, history: history[item.symbol], value: formatNumber(quote.value), change: formatSigned(quote.change), percent: formatPercent(quote.percent), positive: quote.change >= 0, source: quote.source, updatedAt: quote.updatedAt, dataDate: quote.dataDate, previousClose: quote.previousClose, status: quote.status, session: quote.session };
  });
}
function buildAssetItems(quotes: Record<string, ApiQuote>, definitions: Array<{ symbol: string; name: string; category: string; spark: string }>, yieldMode = false, history: Record<string, number[]> = {}): MarketItem[] {
  return definitions.map((definition) => {
    const quote = quotes[definition.symbol] ?? { value: 0, change: 0, percent: 0, source: 'Fallback / Mock', updatedAt: 0, status: 'fallback' as const };
    return { ...definition, history: history[definition.symbol], value: `${formatNumber(quote.value)}${yieldMode ? '%' : ''}`, change: formatSigned(quote.change), percent: yieldMode ? '1D Change' : formatPercent(quote.percent), positive: quote.change >= 0, source: quote.source, updatedAt: quote.updatedAt, dataDate: quote.dataDate, status: quote.status };
  });
}
function buildHistoryPath(values: number[]) { const low = Math.min(...values); const high = Math.max(...values); const spread = high - low || 1; return values.map((value, index) => { const x = 2 + (index / Math.max(values.length - 1, 1)) * 118; const y = 43 - ((value - low) / spread) * 38; return `${index ? 'L' : 'M'}${x.toFixed(2)} ${y.toFixed(2)}`; }).join(' '); }
function formatMacroValue(quote: MacroQuote) { if (quote.unit === 'percent') return `${quote.value.toFixed(2)}%`; if (quote.unit === 'thousands') return `${Math.round(quote.value).toLocaleString('en-US')}K`; if (quote.unit === 'billions') return `$${Math.round(quote.value).toLocaleString('en-US')}B`; return formatNumber(quote.value); }
function statusFor(source: string, updatedAt: number, status?: MarketDataStatus) { if (status) return status; if (!updatedAt || /fallback|mock|模拟/i.test(source)) return 'fallback'; if (/cache/i.test(source)) return 'cache'; return 'real'; }

function BilingualText({ label, className = '' }: { label: BilingualLabel; className?: string }) { return <span className={`bilingual-text ${className}`}><span>{label.en}</span><small>{label.zh}</small></span>; }
function TableHeading({ label }: { label: BilingualLabel }) { return <span className="table-heading"><span>{label.en}</span><small>{label.zh}</small></span>; }
function Sparkline({ path, positive, values }: { path: string; positive: boolean; values?: number[] }) { const historyPath = values && values.length > 1 ? buildHistoryPath(values) : path; return <svg className="sparkline" viewBox="0 0 122 48" aria-hidden="true" preserveAspectRatio="none"><path d={`${historyPath} L120 48 L2 48 Z`} className="sparkline-area" /><path d={historyPath} fill="none" stroke={positive ? 'var(--positive)' : 'var(--negative)'} strokeWidth="2.1" strokeLinecap="round" strokeLinejoin="round" /></svg>; }
function ChartAxis({ item }: { item: MarketItem }) { const values = item.history?.length ? item.history : []; if (!values.length) return null; const low = Math.min(...values); const high = Math.max(...values); const midpoint = (high + low) / 2; const digits = high < 10 ? 2 : high < 100 ? 1 : 0; return <div className="chart-axis" aria-hidden="true"><span>{high.toFixed(digits)}</span><span>{midpoint.toFixed(digits)}</span><span>{low.toFixed(digits)}</span></div>; }

type MarketCardProps = { item: MarketItem; onRemove?: () => void; ticker?: string; primary?: boolean; range?: string; onRangeChange?: (range: string) => void; displayName?: string; };
function MarketCard({ item, onRemove, ticker, primary = false, range, onRangeChange, displayName }: MarketCardProps) {
  const label = assetLabels[item.symbol]; const title = displayName ?? label?.en ?? item.name; const state = statusFor(item.source, item.updatedAt, item.status); const statusLabel = statusLabels[state]; const session = item.session ? sessionLabels[item.session] : null;
  return <article className={`market-card${primary ? ' primary-market-card' : ''}`}><div className="card-topline"><div className="symbol-box">{item.symbol.replace(/\W/g, '').slice(0, 3)}</div><div className="card-label"><h3>{title}</h3><p>{label?.zh ?? item.category}</p></div><div className="card-badges">{session && <span className={`session-badge session-${item.session}`}><b>{session.en}</b><small>{session.zh}</small></span>}{ticker && <span className="ticker-badge">{ticker}</span>}</div>{onRemove && <button className="watch-button" aria-label={`Remove ${item.symbol}`} onClick={onRemove} type="button">×</button>}</div><div className="card-quote"><strong>{item.value}</strong><div className={`change ${item.positive ? 'up' : 'down'}`}><span>{item.positive ? '↑' : '↓'}</span> {item.change} <em>{item.percent}</em></div></div><div className="card-chart"><Sparkline path={item.spark} positive={item.positive} values={item.history} />{primary && <ChartAxis item={item} />}</div>{primary && range && onRangeChange ? <><span className={`card-data-state ${state}`}><i /><span>{statusLabel.en}<small>{statusLabel.zh}</small></span><b>{item.source}</b></span><div className="card-range-tabs">{historyRanges.map((itemRange) => <button className={range === itemRange ? 'selected' : ''} key={itemRange} onClick={() => onRangeChange(itemRange)} type="button">{itemRange}</button>)}</div></> : <div className="card-footer"><span>{item.source}</span><span>·</span><span>{item.dataDate ?? formatTime(item.updatedAt)}</span></div>}</article>;
}

function PanelHeading({ icon, label, href }: { icon: string; label: BilingualLabel; href?: string }) { return <div className="panel-heading"><div><span className="panel-icon" aria-hidden="true">{icon}</span><BilingualText label={label} /></div>{href && <a className="view-more" href={href}><span>View More</span><small>查看更多</small><b>→</b></a>}</div>; }
function MacroTable({ macro }: { macro: Record<string, MacroQuote> }) { return <div className="terminal-table macro-summary-table"><div className="table-row table-head"><TableHeading label={tableLabels.indicator} /><TableHeading label={tableLabels.latest} /><TableHeading label={tableLabels.source} /><TableHeading label={tableLabels.status} /></div>{summaryMacroKeys.map((key) => { const quote = macro[key] ?? fallbackMacroValues[key]; const label = macroLabels[key]; const state = statusFor(quote.source, quote.updatedAt); const status = statusLabels[state]; return <div className="table-row" key={key}><BilingualText label={label ?? { en: key, zh: key }} /><strong>{formatMacroValue(quote)}</strong><small>{quote.source}</small><span className={`status-badge ${state}`}><i /><span>{status.en}<small>{status.zh}</small></span></span></div>; })}</div>; }

function DashboardHeader({ sidebarOpen, onSidebarToggle }: { sidebarOpen: boolean; onSidebarToggle: () => void }) {
  const [now, setNow] = useState(() => new Date());
  useEffect(() => { const timer = window.setInterval(() => setNow(new Date()), 1000); return () => window.clearInterval(timer); }, []);
  const date = new Intl.DateTimeFormat('en-US', { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric' }).format(now); const chineseDate = new Intl.DateTimeFormat('zh-CN', { year: 'numeric', month: 'long', day: 'numeric', weekday: 'short' }).format(now); const time = new Intl.DateTimeFormat('en-GB', { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false }).format(now);
  return <header className="dashboard-header"><button aria-expanded={sidebarOpen} aria-label="Open navigation" className="mobile-nav-toggle" onClick={onSidebarToggle} type="button">☰</button><div className="header-title"><h1>Global Market Dashboard</h1><p>全球市场看板</p></div><div className="header-actions"><div className="date-time"><span>{date}</span><small>{chineseDate}</small><strong>{time}</strong><small>(UTC+8)</small></div><div className="header-search" role="search"><span aria-hidden="true">⌕</span><span>Search symbols, news, or people...</span></div><p className="header-statement">Markets move the world.<br />We help you see it clearly.</p></div></header>;
}

export default function Home() {
  const [range, setRange] = useState('1D'); const [watchlist, setWatchlist] = useState(defaultWatchlist); const [watchlistReady, setWatchlistReady] = useState(false); const [searchSymbol, setSearchSymbol] = useState('');
  const allEquitySymbols = [...new Set([...primaryEquitySymbols, ...watchlist])];
  const [items, setItems] = useState(() => buildItems(fallbackValues, [...new Set([...primaryEquitySymbols, ...defaultWatchlist])])); const [treasuryItems, setTreasuryItems] = useState<MarketItem[]>(() => buildAssetItems({}, treasuryDefinitions, true)); const [cryptoItems, setCryptoItems] = useState<MarketItem[]>(() => buildAssetItems({}, cryptoDefinitions)); const [macro, setMacro] = useState<Record<string, MacroQuote>>(fallbackMacroValues);
  const [errors, setErrors] = useState<string[]>(['Loading market data']); const [loading, setLoading] = useState(false); const [lastUpdated, setLastUpdated] = useState('Awaiting update'); const [sidebarOpen, setSidebarOpen] = useState(false); const refreshRef = useRef<((symbols?: string[], rangeForRequest?: string) => Promise<void>) | null>(null); const pollingRef = useRef(false);
  async function refresh(symbols = watchlist, rangeForRequest = range) { setLoading(true); try { const response = await fetch(`/api/market?symbols=${encodeURIComponent(symbols.join(','))}&range=${rangeForRequest}`, { cache: 'no-store' }); if (!response.ok) throw new Error('Market service unavailable'); const data = (await response.json()) as MarketApiResponse; const equities = [...new Set([...primaryEquitySymbols, ...symbols])]; setItems(buildItems(data.quotes, equities, data.history)); setTreasuryItems(buildAssetItems(data.treasury, treasuryDefinitions, true)); setCryptoItems(buildAssetItems(data.crypto, cryptoDefinitions, false, data.history)); setMacro(data.macro); setErrors(data.errors); setLastUpdated(formatTime(data.fetchedAt)); } catch { const equities = [...new Set([...primaryEquitySymbols, ...symbols])]; setItems(buildItems(fallbackValues, equities)); setTreasuryItems(buildAssetItems({}, treasuryDefinitions, true)); setCryptoItems(buildAssetItems({}, cryptoDefinitions)); setMacro(fallbackMacroValues); setErrors(['Market service unavailable. Structured fallback data is displayed.']); setLastUpdated('Fallback data retained'); } finally { setLoading(false); } }
  useEffect(() => { refreshRef.current = refresh; });
  useEffect(() => { let savedSymbols = defaultWatchlist; try { const stored = window.localStorage.getItem(watchlistStorageKey); const parsed = stored ? JSON.parse(stored) : null; if (Array.isArray(parsed)) savedSymbols = [...new Set(parsed.filter((symbol): symbol is string => typeof symbol === 'string' && symbolPattern.test(symbol.toUpperCase())).map((symbol) => symbol.toUpperCase()))].slice(0, maxWatchlistSize); } catch { savedSymbols = defaultWatchlist; } setWatchlist(savedSymbols); setWatchlistReady(true); void refresh(savedSymbols); }, []);
  useEffect(() => { if (watchlistReady) window.localStorage.setItem(watchlistStorageKey, JSON.stringify(watchlist)); }, [watchlist, watchlistReady]);
  useEffect(() => { if (!watchlistReady) return; const poll = () => { if (!document.hidden && !pollingRef.current && refreshRef.current) { pollingRef.current = true; void refreshRef.current().finally(() => { pollingRef.current = false; }); } }; const interval = window.setInterval(poll, 10_000); document.addEventListener('visibilitychange', poll); return () => { window.clearInterval(interval); document.removeEventListener('visibilitychange', poll); }; }, [watchlistReady]);
  function addToWatchlist(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const symbol = searchSymbol.trim().toUpperCase(); if (!symbolPattern.test(symbol)) return setErrors(['Enter a valid US equity symbol, such as MSFT or AMZN.']); if (watchlist.includes(symbol)) return setErrors([`${symbol} is already in the watchlist.`]); if (watchlist.length >= maxWatchlistSize) return setErrors([`The watchlist supports up to ${maxWatchlistSize} symbols.`]); const nextWatchlist = [...watchlist, symbol]; setWatchlist(nextWatchlist); setSearchSymbol(''); void refresh(nextWatchlist); }
  function removeFromWatchlist(symbol: string) { const nextWatchlist = watchlist.filter((item) => item !== symbol); setWatchlist(nextWatchlist); void refresh(nextWatchlist); }
  function selectRange(nextRange: string) { setRange(nextRange); void refresh(watchlist, nextRange); }
  const overviewItems = items.filter((item) => marketItems.some((market) => market.symbol === item.symbol)); const equityItems = items.filter((item) => allEquitySymbols.includes(item.symbol)); const itemFor = (symbol: string) => [...overviewItems, ...equityItems].find((item) => item.symbol === symbol); const treasuryFor = (symbol: string) => treasuryItems.find((item) => item.symbol === symbol); const cryptoFor = (symbol: string) => cryptoItems.find((item) => item.symbol === symbol);
  const primaryMarkets = [{ item: itemFor('QQQM'), ticker: 'QQQM' }, { item: itemFor('SPYM'), ticker: 'SPYM' }, { item: itemFor('DIA'), ticker: 'DIA' }, { item: itemFor('SMH'), ticker: 'SMH' }, { item: itemFor('VGT'), ticker: 'VGT' }, { item: itemFor('AVGO'), ticker: 'AVGO' }, { item: itemFor('NVDA'), ticker: 'NVDA' }, { item: treasuryFor('US10Y'), ticker: 'US10Y' }, { item: cryptoFor('bitcoin'), ticker: 'BTCUSD' }, { item: itemFor('GC=F'), ticker: 'XAUUSD' }].filter((entry): entry is { item: MarketItem; ticker: string } => Boolean(entry.item));
  const additionalMarkets = overviewItems.filter((item) => !['GC=F'].includes(item.symbol)); const stocks = equityItems.filter((item) => watchlist.includes(item.symbol) && !primaryEquitySymbols.includes(item.symbol)); const additionalMacro = macroDefinitions.filter((definition) => !summaryMacroKeys.includes(definition.key));
  const navItems = [{ href: '#overview', icon: '▦', label: moduleLabels.dashboard, active: true }, { href: '#markets', icon: '⌁', label: moduleLabels.markets }, { href: '#macro', icon: '▤', label: moduleLabels.macroNav }, { href: '#calendar', icon: '▧', label: moduleLabels.calendar }, { href: '#news', icon: '▤', label: moduleLabels.newsNav }, { href: '#people', icon: '♙', label: moduleLabels.keyPeople }, { href: '/dca', icon: '◫', label: moduleLabels.dcaSimulation }, { href: '#settings', icon: '⚙', label: moduleLabels.settings }];
  return <main className="app-shell"><aside className={`sidebar${sidebarOpen ? ' mobile-open' : ''}`}><div className="brand"><div className="brand-mark" aria-hidden="true">◉</div><div><strong>Global Market</strong><small>Insight for a connected world</small></div></div><nav>{navItems.map((entry) => <a className={`nav-item${entry.active ? ' active' : ''}`} href={entry.href} key={entry.href} onClick={() => setSidebarOpen(false)}><span className="nav-icon">{entry.icon}</span><BilingualText className="nav-text" label={entry.label} /></a>)}</nav><div className="sidebar-bottom"><p>Better information<br />for a brighter tomorrow.</p><span className="sidebar-data-status"><i className={errors.length ? '' : 'live'} />{errors.length ? 'Fallback-aware data' : 'Live data available'}</span></div></aside>{sidebarOpen && <button aria-label="Close navigation" className="mobile-sidebar-scrim" onClick={() => setSidebarOpen(false)} type="button" />}<section className="content" id="overview"><DashboardHeader sidebarOpen={sidebarOpen} onSidebarToggle={() => setSidebarOpen((open) => !open)} />{errors.length > 0 && <div className="market-alert"><span>!</span><p>{loading ? 'Loading market data…' : errors[0]}</p><button className="refresh" type="button" onClick={() => void refresh()} disabled={loading}>{loading ? 'Refreshing…' : `Updated ${lastUpdated}`}</button></div>}<section className="primary-markets" id="markets"><div className="section-kicker"><span>◈</span><BilingualText label={moduleLabels.marketOverview} /></div><div className="primary-market-grid">{primaryMarkets.map(({ item, ticker }) => <MarketCard item={item} key={item.symbol} onRangeChange={selectRange} primary range={range} ticker={ticker} />)}</div></section><section className="dashboard-summary-grid"><article className="terminal-panel treasury-summary" id="treasury"><PanelHeading href="#treasury-details" icon="▰" label={moduleLabels.treasuryYields} /><div className="terminal-table"><div className="table-row table-head"><TableHeading label={tableLabels.maturity} /><TableHeading label={tableLabels.yield} /><TableHeading label={tableLabels.oneDayChange} /></div>{treasuryItems.map((item) => <div className="table-row" key={item.symbol}><BilingualText label={assetLabels[item.symbol] ?? { en: item.name, zh: item.category }} /><strong>{item.value}</strong><span className={`change ${item.positive ? 'up' : 'down'}`}>{item.change}</span></div>)}</div></article><article className="terminal-panel crypto-summary" id="crypto"><PanelHeading href="#crypto-details" icon="₿" label={moduleLabels.crypto} /><div className="terminal-table"><div className="table-row table-head"><TableHeading label={tableLabels.symbol} /><TableHeading label={tableLabels.price} /><TableHeading label={tableLabels.oneDayChange} /></div>{cryptoItems.map((item) => <div className="table-row" key={item.symbol}><span className="coin-name"><i>{item.symbol === 'bitcoin' ? '₿' : 'Ξ'}</i><BilingualText label={assetLabels[item.symbol] ?? { en: item.name, zh: item.category }} /></span><strong>{item.value}</strong><span className={`change ${item.positive ? 'up' : 'down'}`}>{item.percent}</span></div>)}</div></article><article className="terminal-panel macro-summary" id="macro"><PanelHeading href="#macro-details" icon="▥" label={moduleLabels.macroIndicators} /><MacroTable macro={macro} /></article></section><section className="dashboard-information-grid"><EconomicCalendar compact /><MarketInformation compact /></section><section className="additional-data" id="markets-details"><div className="compact-section-heading"><BilingualText label={moduleLabels.moreMarkets} /><span>ALL MARKET DATA REMAINS AVAILABLE</span></div><div className="market-grid additional-market-grid">{additionalMarkets.map((item) => <MarketCard item={item} key={item.symbol} ticker={item.symbol === '^DJI' ? 'DJI' : item.symbol === '^GSPC' ? 'SPX' : item.symbol === '^IXIC' ? 'IXIC' : 'WTI'} />)}</div><div className="compact-section-heading" id="macro-details"><BilingualText label={moduleLabels.moreMacro} /><span>OFFICIAL / FALLBACK STATUS PRESERVED</span></div><div className="market-grid macro-detail-grid">{additionalMacro.map((definition) => { const quote = macro[definition.key] ?? fallbackMacroValues[definition.key]; const label = macroLabels[definition.key] ?? { en: definition.name, zh: definition.subtitle }; return <article className="macro-detail-card" key={definition.key}><BilingualText label={label} /><strong>{formatMacroValue(quote)}</strong><div><em className={quote.change >= 0 ? 'up' : 'down'}>{quote.change >= 0 ? '↑' : '↓'} {quote.change >= 0 ? '+' : ''}{quote.change}</em><span>{quote.source}</span></div></article>; })}</div></section><section className="watchlist-section" id="stocks"><div className="compact-section-heading"><BilingualText label={moduleLabels.watchlist} /><form className="watchlist-tools" onSubmit={addToWatchlist}><input aria-label="Add US stock symbol" maxLength={10} onChange={(event) => setSearchSymbol(event.target.value)} placeholder="Add US symbol / 添加美股代码" value={searchSymbol} /><button type="submit">Add <small>添加</small></button></form></div><div className="market-grid watchlist-grid">{stocks.map((item) => <MarketCard item={item} key={item.symbol} onRemove={() => removeFromWatchlist(item.symbol)} ticker={item.symbol} />)}</div></section><footer><span>© 2024 Global Market Dashboard</span><span>Alpaca IEX · Yahoo Finance · Finnhub · US Treasury · Coinbase · BLS · BEA · Federal Reserve</span><span id="settings">v1.0.0</span></footer></section></main>;
}
